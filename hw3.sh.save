#!/bin/bash

# run me with "bash hw3.sh"

if ( test -e "makefile" ) then
	make
	if ( $# -eq	 3 ) then
		./fork2 $1 $2
	elif ( $# -e 5 ) then
		./fork2 $1 $2 $3 $4
	elif ( $# -e 7 ) then
		./fork2 $1 $2 $3 $4 $5 $6
	else
		echo "Unacceptable parameter input"
	fi
else
	echo "No makefile detected"
fi
